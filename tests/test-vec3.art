// ** this test should fail but passes **

int main()
{
    vec v1;

    v1 = <10.5, 5.5>;

    v1[0] = 6.7;
    v1[2] = 77.7; // should fail since vec only 2 elements, but doesn't

    printf(v1[0]);
    printf(v1[1]);
    printf(v1[2]); // should fail but doesn't
    printf(v1[5]); // should fail but doesn't
}